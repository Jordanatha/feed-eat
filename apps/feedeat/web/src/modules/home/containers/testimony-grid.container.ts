import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
    selector: 'feedeat-testimony-grid-container',
    templateUrl: './testimony-grid.container.html',
    changeDetection: ChangeDetectionStrategy.OnPush,
})
export class TestimonyGridContainer {}
